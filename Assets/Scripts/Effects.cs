using UnityEngine;

public class Effects : MonoBehaviour
{
	private void EffectCollision(Transform transform, SpriteRenderer spriteRenderer)
	{
		// _sequence?.Kill();
		// _sequence = DOTween.Sequence();
		// transform.localScale = _defaultTransformScale;
		// spriteRenderer.color = _defaultSpriteRendererColor;
		//
		// _sequence.Append(spriteRenderer.DOColor(new Color(255, 0, 0, 1), 0.05f)
		// 	.SetEase(Ease.InOutExpo)).SetLoops(4, LoopType.Yoyo).SetSpeedBased();
		//
		// _sequence.Append(transform.DOScale(new Vector2(transform.localScale.x * 0.8f, transform.localScale.y * 0.8f), 
		// 	0.05f)).SetLoops(4, LoopType.Yoyo).SetSpeedBased();
	}		
}
